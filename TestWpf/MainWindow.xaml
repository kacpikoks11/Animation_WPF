<Window x:Class="TestWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestWpf"
        mc:Ignorable="d"
        Title="Animation" Height="1080" Width="1920"
        Loaded="StartAnimation"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        WindowState="Maximized"
        Background="{StaticResource BackGroundColor}"
        Foreground="{StaticResource ForeGroundColor}"
        >
    <Grid>
        <Grid.ColumnDefinitions>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
        </Grid.RowDefinitions>

        <Border x:Name="GridBorder"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                >
            <UniformGrid x:Name="AnimationGrid"
                         Width="1800"
                         Height="900"
                        
                         >
                
            </UniformGrid>
            
        </Border>
        <Button Content="Button" HorizontalAlignment="Left" Height="46" Margin="395,10,0,0" VerticalAlignment="Top" Width="139" Click="Reset"/>

    </Grid>
</Window>
